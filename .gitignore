.DS_Store
.env
dashboard/node_modules/
dashboard/dist/
site/node_modules/
site/dist/
/lullmail
/output/

# Dev-only workspace into the live Neutron checkout; deploy vendoring is
# NEUTRON_BUGS.md N4 / TASKS D2. Not committable: clones without the sibling
# would fail every build.
go.work
